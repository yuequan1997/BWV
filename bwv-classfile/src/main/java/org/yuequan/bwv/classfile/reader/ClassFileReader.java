package org.yuequan.bwv.classfile.reader;

public class ClassFileReader extends ByteReader {
    /**
     * Instantiates a new Byte reader.
     *
     * @param bytes the bytes
     */
    public ClassFileReader(byte[] bytes) {
        super(bytes);
    }
}
